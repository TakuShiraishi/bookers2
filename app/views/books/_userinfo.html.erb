 <script src="https://kit.fontawesome.com/d2414ff8ea.js" crossorigin="anonymous"></script>
     <% if book.errors.any? %>
     <%= book.errors.count %>errors prohibited this book from being saved:
      <ul>
       <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
       <% end %>
      </ul>
    <% end %>
    <h2>User info</h2>
    <% if user.profile_image.attached? %>
    <%= image_tag user.get_profile_image(100,100)%>
  <% else %>
    <%= image_tag 'no_image', size: "100x100" %>
  <% end %>
    <table class="table">
    <tbody>
    <tr>
	   <th>name</th>
	    <td><%= user.name %></td>
    </tr>
    <tr>
	  <th>introduction</th>
	 <td><%= user.introduction %></td>
    </tr>
    </tbody>
    </table>

     <%= link_to edit_user_path(user.id), class: 'btn btn-outline-secondary btn-block ' do %>
    <i class="fas fa-user-cog"></i>
    <% end %>
    <h2>New book</h2>
    <%= form_with model: book do |f| %>
    <label for="book_title">Title</label>
      </br><%= f.text_field :title, class: 'form-control book_title' %>
    </br><label for="book_opinion">Opinion</label>
      </br><%= f.text_area :body, class: 'form-control book_body' %>
    <div><%= f.submit 'Create Book'%></div>
    <% end %>

